<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bg51az.comcet325bg51az">

    <permission android:name="com.example.bg51az.comcet325bg51az.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="com.example.bg51az.comcet325bg51az.WRITE_DATABASE" android:protectionLevel="normal"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ListActivity"
            android:label="Tourist List">
        </activity>

        <activity
            android:name=".WeatherActivity"
            android:label="Weather Information">
        </activity>

        <activity
            android:name=".AboutActivity"
            android:label="About">
        </activity>

    </application>

</manifest>